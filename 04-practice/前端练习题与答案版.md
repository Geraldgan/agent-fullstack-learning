# 前端练习题与答案版

> 目标：让你不是只看懂前端知识，而是真的能练、能写、能检查自己是否掌握。  
> 适合对象：iOS 转前端，已开始学习 HTML / CSS / JavaScript / TypeScript / React / Next.js。

---

# 第一部分：HTML / CSS 练习

## 题目 1：写一个登录区域

要求：
- 一个标题“登录”
- 一个用户名输入框
- 一个密码输入框
- 一个登录按钮

### 参考答案
```html
<div class="login-box">
  <h2>登录</h2>
  <input type="text" placeholder="请输入用户名" />
  <input type="password" placeholder="请输入密码" />
  <button>登录</button>
</div>
```

---

## 题目 2：用 CSS 让登录区居中

### 参考答案
```css
.page {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}

.login-box {
  width: 320px;
  padding: 24px;
  border: 1px solid #ddd;
  border-radius: 12px;
}
```

---

## 题目 3：做一个左右布局页面

要求：
- 左边固定 240px
- 右边自适应

### 参考答案
```css
.page {
  display: flex;
  height: 100vh;
}

.sidebar {
  width: 240px;
  border-right: 1px solid #eee;
}

.main {
  flex: 1;
}
```

---

# 第二部分：JavaScript 练习

## 题目 4：把数组中的每个数字乘以 2

### 题目
```js
const nums = [1, 2, 3, 4]
```

### 参考答案
```js
const result = nums.map(item => item * 2)
console.log(result)
```

---

## 题目 5：过滤出大于 10 的数字

### 参考答案
```js
const nums = [5, 10, 15, 20]
const result = nums.filter(item => item > 10)
console.log(result)
```

---

## 题目 6：写一个异步函数请求接口

### 参考答案
```js
async function loadUsers() {
  const res = await fetch('/api/users')
  const data = await res.json()
  console.log(data)
}
```

---

# 第三部分：TypeScript 练习

## 题目 7：定义一个 Message 类型

要求：
- id: string
- role: 'user' | 'assistant'
- content: string

### 参考答案
```ts
interface Message {
  id: string
  role: 'user' | 'assistant'
  content: string
}
```

---

## 题目 8：定义一个通用接口返回结构

### 参考答案
```ts
interface ApiResponse<T> {
  code: number
  message: string
  data: T
}
```

---

# 第四部分：React 练习

## 题目 9：写一个计数器组件

### 参考答案
```tsx
'use client'
import { useState } from 'react'

export default function Counter() {
  const [count, setCount] = useState(0)

  return (
    <div>
      <div>{count}</div>
      <button onClick={() => setCount(count + 1)}>+1</button>
    </div>
  )
}
```

---

## 题目 10：写一个消息列表组件

### 参考答案
```tsx
interface Message {
  id: string
  content: string
}

export default function MessageList({ messages }: { messages: Message[] }) {
  return (
    <div>
      {messages.map(msg => (
        <div key={msg.id}>{msg.content}</div>
      ))}
    </div>
  )
}
```

---

## 题目 11：写一个输入框双向绑定组件

### 参考答案
```tsx
'use client'
import { useState } from 'react'

export default function InputDemo() {
  const [value, setValue] = useState('')

  return (
    <div>
      <input value={value} onChange={e => setValue(e.target.value)} />
      <p>{value}</p>
    </div>
  )
}
```

---

## 题目 12：页面加载后请求数据

### 参考答案
```tsx
'use client'
import { useEffect, useState } from 'react'

export default function UserPage() {
  const [users, setUsers] = useState<any[]>([])

  useEffect(() => {
    async function load() {
      const res = await fetch('/api/users')
      const data = await res.json()
      setUsers(data)
    }
    load()
  }, [])

  return <div>{users.length}</div>
}
```

---

# 第五部分：Next.js 练习

## 题目 13：创建两个页面

要求：
- `/login`
- `/chat`

### 目录示例
```text
src/app/login/page.tsx
src/app/chat/page.tsx
```

### 参考答案
```tsx
export default function LoginPage() {
  return <div>login page</div>
}
```

```tsx
export default function ChatPage() {
  return <div>chat page</div>
}
```

---

# 第六部分：实战综合题

## 题目 14：做一个最小聊天页

要求：
- 一个消息列表
- 一个输入框
- 一个发送按钮
- 点击发送后，把输入内容追加到消息列表

### 参考答案
```tsx
'use client'
import { useState } from 'react'

interface Message {
  id: string
  content: string
}

export default function ChatDemo() {
  const [messages, setMessages] = useState<Message[]>([])
  const [input, setInput] = useState('')

  function handleSend() {
    if (!input.trim()) return

    setMessages(prev => [
      ...prev,
      { id: Date.now().toString(), content: input }
    ])
    setInput('')
  }

  return (
    <div>
      <div>
        {messages.map(msg => (
          <div key={msg.id}>{msg.content}</div>
        ))}
      </div>
      <input value={input} onChange={e => setInput(e.target.value)} />
      <button onClick={handleSend}>发送</button>
    </div>
  )
}
```

---

# 第七部分：自测标准

做完这些题，如果你能做到下面这些，说明前端已经入门：

- 能独立写一个页面结构
- 能用 CSS 做基本布局
- 能写 JS 数组处理和异步请求
- 能写 TS 类型
- 能用 React 管理状态
- 能用 Next.js 建基本页面

你下一步就可以进入真实项目了。
